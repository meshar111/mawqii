# 📦 دليل رفع "مَوقِعي" على GitHub ثم Vercel

## الملفات (١٤ ملفاً)
```
mawqii-full/
├── src/
│   ├── App.jsx          ← الواجهة الكاملة (٥٥٤ سطر)
│   └── main.jsx         ← نقطة التشغيل
├── api/
│   └── analyze.js       ← السيرفر الوسيط (يحمي المفتاح)
├── lib/
│   ├── scoring.js       ← محرّك الدرجة
│   ├── places.js        ← Google Places
│   └── cache.js         ← التخزين المؤقت
├── index.html
├── package.json
├── vite.config.js
├── vercel.json
├── .gitignore           ← يحمي .env
├── .env.example
├── README.md
└── LICENSE
```

---

## الخطوة ١: أنشئ الريبو
افتح **github.com/new**
- الاسم: `mawqii`
- **Public**
- ❌ لا تفعّل "Add a README"
- **Create repository**

## الخطوة ٢: ارفع الملفات

### الطريقة أ — الترمينال (الأضمن)
```bash
cd mawqii-full
git init
git add .
git commit -m "مَوقِعي — منصة ذكاء موقعي"
git branch -M main
git remote add origin https://github.com/USERNAME/mawqii.git
git push -u origin main
```
> بدّل `USERNAME` باسم حسابك

### الطريقة ب — المتصفح
صفحة الريبو ← **uploading an existing file** ← اسحب **كل المحتويات**
> ⚠️ تأكد من سحب المجلدات `src` و `api` و `lib` — وليس الملفات المفردة فقط

## الخطوة ٣: تحقّق
بعد الرفع، تأكد في GitHub من وجود:
- [ ] مجلد `src` فيه ملفان
- [ ] مجلد `api` فيه ملف
- [ ] مجلد `lib` فيه ٣ ملفات
- [ ] `package.json`
- [ ] ❌ **لا يوجد** ملف `.env`

---

## الخطوة ٤: النشر على Vercel
1. ادخل **vercel.com** وسجّل بحساب GitHub
2. **Add New → Project** ← اختر `mawqii` ← **Import**
3. **قبل Deploy:** افتح **Environment Variables** وأضف:
   - Name: `GOOGLE_PLACES_KEY`
   - Value: مفتاحك من Google Cloud
4. **Deploy** ← انتظر دقيقة
5. يطلع رابطك: `mawqii.vercel.app` 🎉

## الخطوة ٥: تحقّق من العمل
افتح الرابط، اختر حياً، واضغط "اقرأ الحي".
- شارة **"بيانات حيّة"** خضراء = ✅ كل شي تمام
- شارة **"نموذج توضيحي"** برتقالية = المفتاح غير مضبوط، راجع Environment Variables

---

## ⚠️ تذكيرات أمنية
- **لا ترفع `.env` أبداً** (محمي في .gitignore)
- قيّد مفتاح Google على **Places API (New)** و **Geocoding API** فقط
- اضبط سقف ميزانية: Billing ← Budgets & alerts ← ٥٠ دولار
